# Firebase Functions

Ten katalog nie jest używany przez aplikację PWA.

Logika backendu (Firestore rules, auth) jest w repozytorium głównym (`firestore.rules`, `js/firebase.js`, `js/auth.js`).

Zostawiony tylko jako placeholder — można usunąć po potwierdzeniu, że Cloud Functions nie są planowane.
